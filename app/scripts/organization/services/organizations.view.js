'use strict';

/**
 * @ngdoc service
 * @name dashboardApp.organizations.view
 * @description
 * # organizations.view
 * Service in the dashboardApp.
 */
angular.module('dashboardApp')
  .service('organizations.view', function () {
    // AngularJS will instantiate a singleton by calling "new" on this function

    var serviceObj={};
/*
  var projects={
    ["id"= 1, "name"="Synerzip"],
    ["id"=2, "name"="PDX"]
    };
    serviceObj.read=function(newTodo){
      this.todos.push(newTodo);


    };*/
  });
